version: "3"
services:
  fe:
    container_name: "resume-fe"
    hostname: "resume-fe"
    build:
      context: .
      dockerfile: Dockerfile
    networks:
      my-network:
        aliases:
          - resume.local
    ports:
      - 80:3000

networks:
  my-network:
